{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd ../client && npm run start",
    "dev": "concurrently --kill-others-on-fail \"node index\" \"npm run client\""
  },
  "author": "Vidushan CHOORIYAKUMARAN",
  "license": "ISC",
  "dependencies": {
    "elasticsearch": "^14.2.0",
    "express": "^4.16.3",
    "node-car-api": "git+https://github.com/92bondstreet/node-car-api.git"
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  }
}
